<div class="row">
  <% flash.each do |type, messages| %>
  <div class="<%= bootstrap_class_for(type) %> fade in">
    <button class="close" data-dismiss="alert">&times;</button>
    <% if messages.respond_to?(:each) %>
    <h4>
      <%= pluralize(messages.count, "error") %> prohibited this brand from being saved:
    </h4>
      <ul>
        <% messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
      </ul>
      <% else %>
        <%= messages %>
    <% end %>
  </div>
  <% end %>
</div>
